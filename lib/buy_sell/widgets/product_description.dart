import 'package:flutter/material.dart';

class description extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Text(
      'These are sem 3 books of DSA ,Maths and DBMS.They are in good condition and have important notes written inside',
      style: TextStyle(
        color: Colors.grey[400],
        fontSize: 20,
      ),
    );
  }
}
